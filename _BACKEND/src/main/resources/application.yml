spring:
  application:
    name: justina
  datasource:
    url: ${DATASOURCE}?createDatabaseIfNotExist=true
    username: ${USER}
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true